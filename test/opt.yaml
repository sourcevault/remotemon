global:
  file: dist/main.js
  proj: utils
  sshport: 45
  first: &first
    - verbose
  second: &second
    - recursive

# remotehost: pi@192.168.43.50

remotehost: pi@192.168.43.47

remotefold: ~/test

watch: true

# verbose: 0

# ssh: -tt -o 'RequestTTY no' -p 45

ssh: -tt -o LogLevel=QUIET -p {{global.sshport}}

foo:
  watch: true
  verbose: 1
  description:
    - writes a long sentence.
  exec-locale:
    - echo 'really long ass sentence, I like ukrainian girls'
  rsync:
    - *first
    - *second
    # - not_valid

    - rsh: ssh -p {{global.sshport}}
    # - rsh: ssh -p 22


    - exclude:
      - node_modules
      - .git

  # - \verbose

  # - &first
  #   - archive
  # - - *second
  #   - verbose
    # - exclude:
    #   - .git

  # - - *first
  #   - *second
  # - verbose


  # - &first
  # - &second



    # - &first
    #   - archive
    #   - exclude:
    #     - .git
    #     - node_modules

    # - - *first
    #   - recursive
    #   - verbose

  # exec-locale:
  #   - exit 1

  # exec-remote:
  #   - echo 'hello world'
    # - sudo rm -rf {{remotefold}}

  # verbose: 0

  # watch: false

# compile.all:
  # exec-finale:
#     - echo
    # - *pkg
    # - lsc -bco dist src
  # - lsc -bc test
    # - node {{global.file}}

    # - lsc -bco dist/{{global.proj}} src/{{global.proj}}
    # - lsc -bc test/{{global.proj}}
    # - node {{global.file}}

# exec-remote:
#   - echo 'hello world'
#   - rm -rf {{remotefold}}

exec-locale:
  - echo 'hello world 3'


#   - rsh: ssh
#   - verbose
#   - recursive
#   - exclude:
#     - .git
#     - node_modules

version.update: update-version src/package.yaml remotemon
