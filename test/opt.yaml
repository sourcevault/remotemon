global:
  file: dist/main.js
  proj: utils
  sshport: 45

# remotehost: pi@192.168.43.50
remotehost: pi@192.168.43.48

remotefold: /etc/joy

watch: true

# verbose: 0

# ssh: -tt -o 'RequestTTY no' -p 45

ssh: -tt -o LogLevel=QUIET -p {{global.sshport}}

pkg:
  # verbose: 1
  # description: writes a long sentence.
  # exec-locale:
  #   - echo 'really long ass sentence, I like ukrainian girls'
  # rsync:
  #   - rsh: ssh -p {{global.sshport}}
  #   - verbose
  #   - recursive
  #   - exclude:
  #     - .git
  #     - node_modules
  # exec-locale:
  #   - exit 1

  exec-remote:
    - echo 'hello world'
    - sudo rm -rf {{remotefold}}
  verbose: 1

  watch: false

# compile.all:
  # exec-finale:
#     - echo
    # - *pkg
    # - lsc -bco dist src
  # - lsc -bc test
    # - node {{global.file}}

    # - lsc -bco dist/{{global.proj}} src/{{global.proj}}
    # - lsc -bc test/{{global.proj}}
    # - node {{global.file}}

# foo:
#   - |-
#     echo {{0}}

# exec-remote:
#   - echo 'hello world'
#   - rm -rf {{remotefold}}

# exec-locale:
#   - echo 'hello world'


# rsync:
#   - rsh: ssh
#   - verbose
#   - recursive
#   - exclude:
#     - .git
#     - node_modules
