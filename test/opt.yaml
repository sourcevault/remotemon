global:
  file: dist/main.js
  proj: utils
  sshport: 45

remotehost: pi@192.168.43.47
# remotehost: pi@192.168.43.41

remotefold: ~/router

watch: package.json

# ssh: -tt -o 'RequestTTY no' -p 45

ssh: -tt -o LogLevel=QUIET -p {{global.sshport}}

pkg:
  # rsync:
  #   - rsh: ssh -p {{global.sshport}}
  #   - verbose
  #   - recursive
  #   - exclude:
  #     - .git
  #     - node_modules
  exec-locale:
    - echo 'really long ass sentence, I like ukrainian girls'
  # exec-remote:
  #   - sudo apt-get update
  # watch: true


# compile.all:
  # exec-finale:
#     - echo
    # - *pkg
    # - lsc -bco dist src
  # - lsc -bc test
    # - node {{global.file}}

    # - lsc -bco dist/{{global.proj}} src/{{global.proj}}
    # - lsc -bc test/{{global.proj}}
    # - node {{global.file}}

# foo:
#   - |-
#     echo {{0}}

