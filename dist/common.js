// Generated by LiveScript 1.6.0
(function(){
  var jsYaml, render, fs, child_process, R, optionator, chokidar, chalk, validator, l, j, readYaml, readJson, z, main;
  jsYaml = require('js-yaml');
  render = require('json-stringify-pretty-compact');
  fs = require("fs");
  child_process = require("child_process");
  R = require("ramda");
  optionator = require("optionator");
  chokidar = require("chokidar");
  chalk = require("chalk");
  validator = require("is-my-json-valid");
  l = console.log;
  j = function(json){
    return l(render(json));
  };
  readYaml = function(name){
    return jsYaml.safeLoad(fs.readFileSync(name));
  };
  readJson = function(filename){
    return JSON.parse(
    R.toString(
    fs.readFileSync(filename)));
  };
  z = l;
  main = {
    render: render,
    chalk: chalk,
    j: j,
    z: z,
    fs: fs,
    R: R,
    child_process: child_process,
    l: l,
    readYaml: readYaml,
    jsYaml: jsYaml,
    optionator: optionator,
    chokidar: chokidar,
    readJson: readJson
  };
  module.exports = main;
}).call(this);
